- Itens que Faltam para a Finalização da Calculadora:


- Usar o modo de cálculo informado pelo felipe para "chamar" a calculadora;
- Eliminar das constantes e parâmetros os parâmetros que não forem usados por funções exigidas pela calculadora;
- Pensar em uma solução para a normal truncada quando o desvio é zero. Talvez valha a pena mudar para a normal "sem ser truncada", se o desvio for menor do que 0,000001, ou algo assim. Desta maneira o erro da normal truncada será minimizado.


template de dados ajustado (colunas de cenários).


- Eventos Arbitrados estão usando dados do baseline, mesmo que tenham sido arbitrados.




3) Remover da entrada de dados (principalmente parâmetros) as variáveis que são inputs para funções que não serão calculadas (dessa maneira há mais uma garantia que a calculadora não irá parar por funções desnecessárias.

Retirar Nome "Cenario.y" do Relatório.

4) Função do Dieter não funciona em todos os casos.

3) Integrar ao Shiny.


## Chamada da Função para Executar a Biblioteca (Esta funcao deve ser chamada quando os objetos de tratamento de dados já existem).
oshcba::simular_cba(ArquivoInputs = obter_inputs_list_dados_tratados(list_dados_tratados = gerar_list_dados_tratados()),
tipo_input = "list", 
modo = "basico")

4) Integrar o Relatório ao Shiny.


### Feito ###

OK - 1) Relatório RMD: Manter apenas itens que sejam automatizáveis no relatório, fazer o gráfico funcionar sem precisar repetir o código.

OK - 2) Retirar a dependência do template de dados a um arquivo excel. Importar como um objeto de dados do R.
